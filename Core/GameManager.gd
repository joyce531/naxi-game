extends Node

## Central flow director + quiz session state.
## Full flow list, advance(), and the wrong-only retry tracker are implemented
## in the "core" phase. This setup-phase stub only proves the autoload loads and
## lets the MainMenu button be wired.

func start_game() -> void:
	print("[GameManager] start_game() called - flow not implemented yet (setup phase).")
